version: "3.8"
services:
  frontend:
    build: .
    ports:
      - "8080:80"
    environment:
      - VITE_API_URL=${VITE_API_URL:-http://host.docker.internal:3000}
      - VITE_LIST_ID=${VITE_LIST_ID:-1}
